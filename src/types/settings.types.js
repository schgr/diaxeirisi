/**
 * Shared shape reference for settings payloads.
 * Kept as JS typedefs so the prototype stays dependency-light.
 *
 * @typedef {Object} ServiceInfo
 * @property {string} serviceName
 *
 * @typedef {Object} FinancialOfficers
 * @property {string} commander
 * @property {string} ped
 * @property {string} manager
 */
